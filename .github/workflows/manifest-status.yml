name: Manifest status validation

on: [push]

permissions:
  contents: read

jobs:
  manifest-status:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
      - run: node scripts/check-manifest-status.js
